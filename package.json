{
  "name": "build2trade-dashboard",
  "version": "1.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --dir apps/admin-portal dev",
    "build": "pnpm --dir apps/admin-portal build",
    "start": "pnpm --dir apps/admin-portal start",
    "lint": "pnpm --dir apps/admin-portal lint",
    "ui:add": "pnpm --filter @repo/ui ui:add",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "prettier": "^3.1.1"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
